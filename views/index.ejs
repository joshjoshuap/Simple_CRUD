<!DOCTYPE html>
<html lang="en">
  <%- include('partials/header')%>
  <body>
    <!-- Table Data -->

    <div class="d-flex vh-100 justify-content-center align-items-center">
      <div class="container">
        <div class="row">
          <div class="col">
            <table class="table table-hover text-center">
              <thead class="text-center">
                <tr>
                  <th colspan="9" class="text-center">
                    <h1>USER INFORMATION</h1>
                  </th>
                </tr>
              </thead>

              <thead>
                <tr>
                  <th>Username</th>
                  <th>Password</th>
                  <th>Firstname</th>
                  <th>Lastname</th>
                  <th>Age</th>
                  <th>Address</th>
                  <th>Contact</th>
                  <th colspan="2">Action</th>
                </tr>
              </thead>

              <% for (let user of users) { %>
              <tbody>
                <tr>
                  <td><%= user.username %></td>
                  <td><%= user.password %></td>
                  <td><%= user.firstName %></td>
                  <td><%= user.lastName %></td>
                  <td><%= user.age %></td>
                  <td><%= user.address %></td>
                  <td><%= user.contact %></td>
                  <td>
                    <!-- Trigger app.get("/:id/edituser") in app.js-->
                    <a href="/<%= user._id %>/edituser">
                      <button class="btn btn-md btn-primary">Edit</button>
                    </a>
                  </td>
                  <td>
                    <!-- Trigger app.delete("/:id") in app.js-->
                    <form
                      action="/<%= user._id %>?_method=DELETE"
                      method="POST"
                    >
                      <button class="btn btn-md btn-danger">Delete</button>
                    </form>
                  </td>
                </tr>
              </tbody>

              <% } %>
            </table>
          </div>
        </div>

        <div class="row">
          <div class="col text-center">
            <!-- Trigger app.get("/adduser") in app.js-->
            <a href="/adduser">
              <button class="btn btn-lg btn-success">Add User</button>
            </a>
          </div>
        </div>
      </div>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-U1DAWAznBHeqEIlVSCgzq+c9gqGAJn5c/t99JyeKa9xxaYpSvHU5awsuZVVFIhvj"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
